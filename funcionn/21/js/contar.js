/**
 * funcion contar de 1 al 5 con for 
 * autor: ingrid medina 
 * fecha: 3 de abril del 2024
*/

let numeros;
const contarExp = function (pnumero){
    numeros = pnumero;
    let contador;
    let numero = 5;
    let resultado = "";
    for(contador = 1; numero >= contador; contador ++){
        resultado += contador+".\n";
    }
    return resultado;
}
function contar(pnumero){
    numeros = pnumero;
    let contador;
    let numero = 5;
    let resultado = "";
    for(contador = 1; numero >= contador; contador ++){
        resultado += contador+".\n";
    }
    return resultado;
}