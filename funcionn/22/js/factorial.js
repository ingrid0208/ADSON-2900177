/**
 * funcion imprima el factorial de 5 con for 
 * autor: ingrid medina 
 * fecha: 3 de abril del 2024
*/

let numero;
const factorExp = function (pnumero){

    numero = pnumero;
    let factorial = 1;
    let contador;
    for(contador = 1;  numero >= contador; contador++){

        factorial = factorial * contador;
    }
    return factorial;
}
function factor(pnumero){

    numero = pnumero;
    let factorial = 1;
    let contador;
    for(contador = 1;  numero >= contador; contador++){

        factorial = factorial * contador;
    }
    return factorial;
}