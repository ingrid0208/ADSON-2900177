/**
 * funcion imprimir el porcentaje de un numero
 * autor: ingrid medina 
 * fecha: 3 de abril del 2024
 */

let numUno;

const porcientoExp = function(pnumeroUno,){
    let porcientos;
    numUno = pnumeroUno;
    porcientos = numUno / 100;
    return porcientos;
}
function porciento(pnumeroUno,) {
    let porcientos;
    numUno = pnumeroUno;
    porcientos = numUno / 100;
    return porcientos;
}